name: Python build

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:    
    # run linter
    - name: Lint with flake8
      run:
        pip install poetry
        make install
        make lint  
    # run pytest
    - name: Test with pytest
      run:
        pip install poetry
        make install
        make lint